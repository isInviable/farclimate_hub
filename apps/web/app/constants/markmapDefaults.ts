/**
 * Default YAML front matter passed to markmap before markdown body.
 * Keep in sync with `MarkmapViewer` when changing markmap styling defaults.
 */
export const DEFAULT_MARKMAP_YAML = `--- 
title: my title 
markmap:
  colorFreezeLevel: 2
  color: '#2980b9'
--- 
`;
